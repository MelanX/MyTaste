server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        return 502;
    }

    location /uploads/ {
        return 502;
    }

    location / {
        try_files $uri /index.html;
    }
}
