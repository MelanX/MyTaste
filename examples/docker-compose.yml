services:
  mytaste:
    image: ghcr.io/melanx/mytaste:latest
    ports:
      - '3000:5000'
    environment:
      - REQUIRE_LOGIN=true
      - ADMIN_USER=admin
      - ADMIN_PASS=adm1n
      - JWT_SECRET=abaWyk9m2GfUr0gy9Q9keZan5kjfRSxnRlrQYCt3NnDDc7Fu5aVRy3f92No42mPN
      - JWT_REFRESH_SECRET=Ke4rRUDKpPg0lu376mKU54mLUuAA37UUGPn2wMK1XucUDAP9OgBoSPRihCYKbFjt
      - ALLOWED_ORIGINS=http://localhost
    volumes:
      - /docker/mytaste/data:/app/data # Where recipes and config are stored
      - /docker/mytaste/uploads:/app/uploads # Where uploaded images will end up
    restart: always
