services:
  frontend:
    image: ghcr.io/melanx/mytaste-frontend:latest
    ports:
      - '3000:80'
    environment:
      - VITE_API_URL=https://example.com # public backend URL
    restart: always
